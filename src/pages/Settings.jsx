import React from "react";
import { Typography } from "@mui/material";

const Settings = () => {
    return (
        <div>
            <Typography variant="h4" color="text.secondary">Configurações</Typography>
            <Typography variant="body1" color="text.secondary">Aqui você pode configurar seu sistema.</Typography>
        </div>
    );
};

export default Settings;
