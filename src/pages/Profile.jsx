import React from "react";
import { Typography } from "@mui/material";

const Profile = () => {
    return (
        <div>
            <Typography variant="h4" color="text.secondary">Perfil</Typography>
            <Typography variant="body1" color="text.secondary">Aqui está o seu perfil de usuário.</Typography>
        </div>
    );
};

export default Profile;
